# name: ci
# on:
#   push: { branches: [main] }
#   pull_request:
# jobs:
#   backend:
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:16-alpine
#         env:
#           POSTGRES_USER: umira
#           POSTGRES_PASSWORD: umira
#           POSTGRES_DB: umira
#         ports: ["5432:5432"]
#         options: >-
#           --health-cmd="pg_isready -U umira" --health-interval=5s --health-timeout=5s --health-retries=10
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with: { node-version: "20" }
#       - name: Install
#         working-directory: backend
#         run: npm install
#       - name: Prisma generate
#         working-directory: backend
#         run: npx prisma generate
#         env:
#           DATABASE_URL: postgresql://umira:umira@localhost:5432/umira?schema=public
#       - name: Build
#         working-directory: backend
#         run: npm run build
#         env:
#           DATABASE_URL: postgresql://umira:umira@localhost:5432/umira?schema=public
#           JWT_SECRET: ci_secret_must_be_at_least_thirty_two_characters_long
#   mobile:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: subosito/flutter-action@v2
#         with: { flutter-version: "3.22.0", channel: "stable" }
#       - working-directory: mobile
#         run: |
#           flutter pub get
#           flutter analyze
